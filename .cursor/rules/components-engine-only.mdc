---
description: packages/components 仅做引擎层与目录分层约束
globs: packages/components/**/*
alwaysApply: false
---

# Components Engine-Only Rule

## 适用范围
- 仅在 `packages/components` 目录内生效。
- 目标是先完成可复用、可测试、与框架解耦的引擎层实现。

## 目录结构约束（先分层再编码）
- 新功能优先拆分为以下层级：
  - `core/`：纯算法、解析、转换、协议、类型定义。
  - `engine/`：编排流程与能力聚合，不直接依赖框架运行时。
  - `adapters/`：可选适配层，用于对接 Vue/React/DOM/Node 等外部环境。
  - `ui/`：仅放展示组件，不承载核心业务逻辑。
- 若当前目录已有既定结构，遵循现有命名风格，并按“核心逻辑下沉到 `core/` 或 `engine/`”原则重构。

## 导出边界约束（UI 必须单独导出）
- 默认入口（如 `index.ts`、各模块 `index.ts`）只允许导出引擎层能力，不允许导出 `ui/` 目录组件。
- UI 组件统一从独立入口导出（如 `vue-ui.ts`），禁止与引擎层能力混合导出。
- 若发现已有混导，优先执行拆分：
  1. 保留默认入口的 `core/`、`engine/`、类型导出。
  2. 将 `ui/` 组件迁移到专用 UI 入口。
  3. 保持 API 变更最小化并在变更说明中标注迁移路径。

## 引擎层开发原则
- 默认只实现引擎层（`core/` + `engine/`）能力。
- 引擎层代码要求：
  - 无框架特定 API（如 Vue 生命周期、插件注册、全局实例注入等）。
  - 通过接口/参数注入外部依赖，避免硬编码框架对象。
  - 可在独立单测环境运行。
- 注释使用 JSDoc 风格，重点说明输入、输出、边界条件与副作用。

## 框架接入门禁（必须询问）
- 发现以下“框架级”行为时，不直接接入：
  - 引入框架运行时对象或插件机制。
  - 修改框架全局配置、路由、状态容器、渲染入口。
  - 将引擎逻辑直接耦合到组件生命周期。
- 正确流程：
  1. 先完成引擎层代码与必要类型定义。
  2. 明确列出接入点、影响面和最小改动方案。
  3. 向用户提问“是否现在接入框架层”，得到确认后再实施。

## 交付要求
- 每次实现优先提交：
  - 引擎层代码
  - 类型定义
  - 基础测试或可验证示例
- 若任务包含 UI 或框架改造，先交付引擎层，再等待用户确认接入。
